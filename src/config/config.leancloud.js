// Generated by CoffeeScript 2.2.3
var classes, urlConf,
  _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; },
  indexOf = [].indexOf;

import LC_Key from '../../lcKey';

classes = ['Person', 'Landlords', 'Tenants', 'Todos', 'Notices', 'Houses', 'Rooms', 'Beds', 'Rents', 'Orders'];

urlConf = {
  withSSL: true,
  host: `${LC_Key.id.slice(0, 8).toLowerCase()}.api.lncld.net`,
  prefix: ({business}) => {
    var base;
    base = '1.1';
    return (Object.keys(business)).reduce((r, c) => {
      return _extends({}, r, {
        [`${c}`]: indexOf.call(classes, c) >= 0 ? `${base}/classes` : base
      });
    }, {});
  },
  headers: {
    "X-LC-Id": LC_Key.id,
    "X-LC-Key": LC_Key.key
  }
};

export {
  classes,
  urlConf
};
